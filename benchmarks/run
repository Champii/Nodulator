#!/usr/bin/env bash

echo
MW=$1 lsc $2 &
pid=$!

sleep 2

wrk 'http://localhost:3000/api/1/players' \
  -d 30 \
  -c 400 \
  -t 8 \
  # -R 2000
  # | grep 'Requests/sec' \
  # | awk '{ print "  " $2 }'

kill $pid
